## Packages
framer-motion | For smooth page transitions and game card hover effects
lucide-react | Icon system for navigation and UI elements
date-fns | Formatting dates in transaction history
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes

## Notes
- Images for games are external URLs provided in the schema
- Authentication uses Replit Auth (backend provided)
- File uploads for deposit proof are simulated (mock URL) as per instructions
- Tailwind config needs to handle custom Gold/Red/Black theme
